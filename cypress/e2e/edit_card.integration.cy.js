// negative tests
describe('Edit card tests', () => {
    beforeEach(() => {
        // save a backup file

        // grab data
    })
})

// enter nothing in input box front, click save - should give error

// enter nothign in input box back, click save - should give error

// enter very very long string - should have max chars

// enter very short string - should still work

// enter only front - should still work

// enter only back - should still work

// enter different characters - should still work